import React from "react";
import data from "../../data.json";

// for this component, we should accept an array of 3 slugs as props.
// Call findFeaturedProducts() function and pass that array of slugs in so that we can
// pull all the product objects out and use them inside this component.
